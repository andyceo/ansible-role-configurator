---

configurator:

  default:
    directory:
      mode: 755
      owner: root
      group: root
    file:
      mode: 644
      owner: root
      group: root
      force: True
    symlink:
      force: False

  directories:

    # Base data directory
    - dir: /data

  files: []

  symlinks: []
