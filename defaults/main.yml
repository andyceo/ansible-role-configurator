---

configurator:

  default:
    directory:
      mode: 755
      owner: root
      group: root
      state: directory
    file:
      mode: 644
      owner: root
      group: root
      force: True

  directories:

    # Base data directory
    - dir: /data

  files: []
