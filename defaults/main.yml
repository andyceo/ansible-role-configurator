---

configurator:

  default:
    rsyncdir:
      archive: yes
      notify: []
    fulldir:
      owner: root
      group: root
      force: True
      notify: []
    directory:
      mode: "u=rwx,g=rx,o=rx"  # 755
      owner: root
      group: root
      state: directory
      notify: []
      possible_states:
        - directory
        - absent
    file:
      mode: "u=rw,g=r,o=r"  # 644
      owner: root
      group: root
      force: True
      notify: []
      backup: True
    symlink:
      force: False
      notify: []
    template:
      mode: "u=rw,g=r,o=r"  # 644
      owner: root
      group: root
      force: True
      notify: []
      backup: True

  delete: {}

  rsyncdirs: {}

  fulldirs: {}

  directories: {}

  files: []

  templates: {}

  symlinks: []
